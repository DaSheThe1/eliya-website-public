# Claude instructions — eliya-thebeautybrand

Follow `AGENTS.md`, the frozen brief, and the exact task packet. For frontend
tasks, create your own worktree and task branch from the packet's full base
commit, keep API/config contracts unchanged, and limit edits to the stated
allowlist. Return schema-valid handoff JSON in your response after validation;
do not write the handoff into the task worktree. Do not deploy or merge.
